import { HomeView } from 'src/sections/home/view';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Crypto GPT: Home',
};

export default function Page() {
    return <HomeView />;
}
