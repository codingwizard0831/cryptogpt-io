import { DashboardStrategyCreateView } from 'src/sections/strategy/view';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Dashboard: Create New Strategy',
};

export default function Page() {
    return <DashboardStrategyCreateView />;
}
