import { FilesView } from 'src/sections/overview/overview-files';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: My Agents',
};

export default function Page() {
  return <FilesView />;
}
