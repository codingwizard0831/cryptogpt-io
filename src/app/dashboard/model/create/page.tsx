import { ModelCreateView } from 'src/sections/overview/overview-model-create';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Dashboard: Model Create',
};

export default function Page() {
    return <ModelCreateView />;
}
