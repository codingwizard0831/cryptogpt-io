import { ModelUpdateView } from 'src/sections/overview/overview-model-update';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Dashboard: Model Update',
};

export default function Page() {
    return <ModelUpdateView />;
}
