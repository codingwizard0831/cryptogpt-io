import { ProfileAddressView } from 'src/sections/deliver/view';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Dashboard: Deliver - Profile Order Again View',
};

export default function Page() {
    return <ProfileAddressView />;
}
