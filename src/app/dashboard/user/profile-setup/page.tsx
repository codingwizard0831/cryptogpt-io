import ProfileSetup from 'src/sections/users/profile-setup/profile-setup';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Profile Setup',
};

export default function Page() {
  return <ProfileSetup />;
}
