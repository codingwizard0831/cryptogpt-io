import { JwtPhoneVerifyView } from 'src/sections/auth/jwt';

// ----------------------------------------------------------------------

export const metadata = {
    title: 'Auth: Phone Verify',
};

export default function EmailVerifyPage() {
    return <JwtPhoneVerifyView />;
}
